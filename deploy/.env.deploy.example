# Переменные для скриптов деплоя (scripts/).
# Скопируйте в .env.deploy и заполните. Загрузите перед запуском:
#   Get-Content deploy/.env.deploy | ForEach-Object { if ($_ -match '^([^#][^=]+)=(.*)$') { [Environment]::SetEnvironmentVariable($matches[1].Trim(), $matches[2].Trim(), 'Process') } }
#
# SSH: пароль в env не поддерживается. Настройте SSH-ключ: docs/SSH_SYNOLOGY.md

# NAS 192.168.10.55:7022
DEPLOY_SSH_HOST=192.168.10.55
DEPLOY_SSH_PORT=7022
DEPLOY_SSH_KEY_PATH=C:/Users/<user>/.ssh/id_ed25519_nas_deploy
DEPLOY_SSH_USER=admin
DEPLOY_REMOTE_PATH=/volume1/docker/telegram-anytype-service

# Docker Hub (для push)
DOCKERHUB_USERNAME=rast53
DOCKERHUB_TOKEN=

# Smoke-тесты (URL сервиса после деплоя)
SMOKE_BASE_URL=http://192.168.10.55:3000
